# 🚀 Vim 핵심 명령어 및 키 조합 요약

Vim 명령어는 크게 **노멀 모드(Normal Mode)**와 **명령 모드(Command Mode)**에서 사용됩니다.

## I. 모드 전환 및 기본 조작

| 명령어 | 모드 | 기능 | 설명 |
| :--- | :--- | :--- | :--- |
| **`Esc`** | **입력 → 노멀** | 입력/비주얼 모드를 **종료**하고 노멀 모드로 돌아갑니다. |
| **`i`** | **노멀 → 입력** | 커서 **앞**에서 입력 모드를 시작합니다. (`i`nsert) |
| **`a`** | **노멀 → 입력** | 커서 **뒤**에서 입력 모드를 시작합니다. (`a`ppend) |
| **`o`** | **노멀 → 입력** | 현재 줄 **아래**에 새 줄을 만들고 입력 모드를 시작합니다. |
| **`O`** | **노멀 → 입력** | 현재 줄 **위**에 새 줄을 만들고 입력 모드를 시작합니다. |
| **`u`** | **노멀** | 마지막 명령을 **실행 취소** (Undo) 합니다. |
| **`Ctrl` + `r`** | **노멀** | 실행 취소한 명령을 **다시 실행** (Redo) 합니다. |
| **`.`** | **노멀** | **마지막 변경 명령**을 반복합니다. |

---

## II. 커서 이동 및 점프 (Motion)

| 명령어 | 모드 | 기능 | 설명 |
| :--- | :--- | :--- | :--- |
| **`h`, `j`, `k`, `l`** | 노멀 | 각각 좌, 아래, 위, 오른쪽으로 한 문자 이동 |
| **`w`** | 노멀 | 다음 **단어** 시작 위치로 이동 |
| **`e`** | 노멀 | 현재/다음 **단어 끝** 위치로 이동 |
| **`b`** | 노멀 | **이전 단어** 시작 위치로 이동 |
| **`0`** | 노멀 | 현재 줄의 **첫 번째 문자** 위치로 이동 |
| **`^`** | 노멀 | 현재 줄의 **첫 번째 비공백 문자** 위치로 이동 |
| **`$`** | 노멀 | 현재 줄의 **마지막 문자** 위치로 이동 |
| **`gg`** | 노멀 | 파일의 **맨 위** (첫 줄)로 이동 |
| **`G`** | 노멀 | 파일의 **맨 아래** (마지막 줄)로 이동 |
| **`[숫자]G`** | 노멀 | 특정 **줄 번호**로 이동 (예: `5G`는 5번째 줄로 이동) |
| **`Ctrl` + `f`** | 노멀 | **한 페이지 아래**로 스크롤 (Forward) |
| **`Ctrl` + `b`** | 노멀 | **한 페이지 위**로 스크롤 (Backward) |
| **`Ctrl` + `o`** | 노멀 | **이전 점프 위치**로 돌아가기 |
| **`Ctrl` + `i`** | 노멀 | **다음 점프 위치**로 이동 (Redo Jump) |

---

## III. 삭제, 복사, 변경 (Yank, Delete, Change)

Vim 명령어는 **[횟수] + [동작(Operator)] + [이동(Motion)]** 구조를 가집니다.

| 명령어 | 모드 | 기능 | 설명 |
| :--- | :--- | :--- | :--- |
| **`x`** | 노멀 | 커서가 위치한 **한 문자** 삭제 |
| **`dd`** | 노멀 | 현재 **한 줄** 삭제 (잘라내기) |
| **`[숫자]dd`** | 노멀 | 지정된 수만큼 **줄** 삭제 (예: `5dd`는 5줄 삭제) |
| **`dw`** | 노멀 | 커서부터 **단어 끝까지** 삭제 |
| **`d$`** | 노멀 | 커서부터 **줄 끝까지** 삭제 |
| **`yy`** | 노멀 | 현재 **한 줄** 복사 (Yank) |
| **`[숫자]yy`** | 노멀 | 지정된 수만큼 **줄** 복사 (예: `3yy`는 3줄 복사) |
| **`p`** | 노멀 | 레지스터 내용 **커서 뒤**에 붙여넣기 |
| **`P`** | 노멀 | 레지스터 내용 **커서 앞**에 붙여넣기 |
| **`cw`** | 노멀 | 커서부터 단어 끝까지 **삭제하고 입력 모드** 전환 (Change Word) |
| **`cc`** | 노멀 | 현재 줄 **전체를 삭제**하고 입력 모드 전환 |
| **`r`** | 노멀 | 현재 문자 하나를 **바꾸고** 노멀 모드 유지 (Replace) |
| **`J`** | 노멀 | 현재 줄과 다음 줄을 **합치기** (Join) |

---

## IV. 비주얼 모드 (선택)

| 명령어 | 모드 | 기능 | 설명 |
| :--- | :--- | :--- | :--- |
| **`v`** | 노멀 → 비주얼 | **문자 단위** 선택 모드 시작 |
| **`V`** | 노멀 → 비주얼 | **줄 단위** 선택 모드 시작 |
| **`Ctrl` + `v`** | 노멀 → 비주얼 | **블록 단위** (사각형 영역) 선택 모드 시작 |
| **`y`** | 비주얼 | 선택 영역 복사 (Yank) |
| **`d`** | 비주얼 | 선택 영역 삭제 |
| **`=`** | 비주얼 | 선택 영역의 **자동 들여쓰기** |

---

## V. 명령 모드 (Command Mode)

콜론(`:`)을 눌러 진입하며, 파일 관리 및 고급 편집에 사용됩니다.

| 명령어 | 용도 | 기능 |
| :--- | :--- | :--- |
| **`:w`** | 파일 관리 | 현재 파일을 저장합니다. |
| **`:wq`** | 파일 관리 | 저장하고 종료합니다. |
| **`:q!`** | 파일 관리 | 저장하지 않고 강제 종료합니다. |
| **`:e [파일명]`** | 파일 관리 | 새 파일을 엽니다. |
| **`:sp`** 또는 **`:split`** | 창 분할 | 현재 파일을 **수평**으로 분할합니다. |
| **`:vsp`** 또는 **`:vsplit`** | 창 분할 | 현재 파일을 **수직**으로 분할합니다. |
| **`:close`** | 창 분할 | 현재 분할 창을 닫습니다. |
| **`:set [옵션]`** | 환경 설정 | Vim 옵션을 설정합니다 (예: `:set number`). |
| **`:set no[옵션]`** | 환경 설정 | Vim 옵션을 해제합니다 (예: `:set nonumber`). |
| **`:%s/old/new/g`** | 검색/대체 | 파일 전체에서 `old`를 `new`로 모두 대체합니다. |
| **`:![쉘 명령]`** | 쉘 명령 | Vim을 나가지 않고 쉘 명령을 실행하고 결과를 화면에 출력합니다. (예: `:!ls`) |
| **`:r![쉘 명령]`** | 쉘 명령 | 쉘 명령의 **출력 결과**를 커서 아래에 삽입합니다. (예: `:r!date`) |
| **`:[range]![command]`** | 쉘 명령 | 특정 범위의 텍스트를 쉘 명령의 입력으로 전달하고, 그 출력으로 텍스트를 교체합니다. (예: `:%!sort`로 파일 전체 정렬) |

---

## VI. 유용한 키 조합 및 설정 옵션

| 명령어 | 모드 | 분류 | 기능 |
| :--- | :--- | :--- | :--- |
| **`*`** | 노멀 | 검색 | 커서 아래 단어를 검색하고 다음 일치 항목으로 이동 |
| **`#`** | 노멀 | 검색 | 커서 아래 단어를 검색하고 이전 일치 항목으로 이동 |
| **`:set hlsearch`** | 명령 | 설정 | 검색 결과 하이라이트 **활성화** |
| **`:set incsearch`** | 명령 | 설정 | **점진적 검색** 활성화 (입력 중 결과 표시) |
| **`:set ignorecase`** | 명령 | 설정 | 검색 시 **대소문자 무시** |
| **`:set smartcase`** | 명령 | 설정 | 대문자가 있으면 구분하고, 없으면 무시 |
| **`:set tabstop=[n]`** | 명령 | 설정 | 탭 문자의 너비를 **공백 n개**로 설정 |
| **`:set expandtab`** | 명령 | 설정 | 탭 키 입력 시 탭 문자 대신 **공백** 사용 |
| **`Ctrl` + `w` `w`** | 노멀 | 분할 창 | 다음 분할 창으로 **순환 이동** |
| **`Ctrl` + `w` $\mathbf{=}$** | 노멀 | 분할 창 | 모든 분할 창의 **크기를 균등화** |


-----

## 🛠️ `.vimrc` 파일 설정 방법

### 1\. `.vimrc` 파일 위치

`.vimrc` 파일은 보통 사용자 홈 디렉터리(`~`)에 위치해야 합니다.

  * **리눅스/macOS:** `~/.vimrc` 또는 `~/.config/nvim/init.vim` (Neovim의 경우)
  * **Windows:** `_vimrc` (Vim 설치 폴더 또는 사용자 홈 디렉터리)

파일이 없다면, `vim ~/.vimrc` 명령으로 파일을 생성하고 편집하면 됩니다.

### 2\. 설정 문법

`.vimrc` 파일 내에서는 명령 모드에서 사용하던 콜론(`:`) 없이 바로 옵션 명령을 사용합니다.

| 일반 명령 모드 | `.vimrc` 파일 내 |
| :--- | :--- |
| `:set number` | `set number` |
| `:set hlsearch` | `set hlsearch` |

-----

## ⚙️ `.vimrc`에 포함해야 할 필수 기본 옵션

다음은 대부분의 Vim 사용자들이 생산성 향상을 위해 `.vimrc`에 필수로 설정하는 옵션들입니다.

### 1\. 🔍 검색 및 하이라이트

| 설정 | 설명 |
| :--- | :--- |
| `set hlsearch` | **검색 결과**를 강조(하이라이트)하여 표시합니다. |
| `set incsearch` | **점진적 검색**을 활성화하여 입력과 동시에 결과를 보여줍니다. |
| `set ignorecase` | 기본적으로 검색 시 **대소문자를 무시**합니다. |
| `set smartcase` | `ignorecase`가 켜져 있을 때, 검색어에 **대문자가 포함**되면 대소문자를 구분합니다. |

### 2\. 📝 화면 및 외관

| 설정 | 설명 |
| :--- | :--- |
| `set number` | 각 줄의 **줄 번호**를 표시합니다. |
| `set ruler` | 화면 오른쪽 하단에 **커서의 위치**(`[줄:열]`)를 표시합니다. |
| `set laststatus=2` | 상태 표시줄(Status Line)을 **항상 표시**합니다. |
| `syntax on` | \*\*구문 강조(Syntax Highlighting)\*\*를 활성화합니다. |

### 3\. 탭 및 들여쓰기

대부분의 코딩 표준은 탭 대신 공백(Space) 4칸을 사용하도록 권장합니다.

| 설정 | 설명 |
| :--- | :--- |
| `set tabstop=4` | 탭 문자(`\t`)의 너비를 **4칸**으로 설정합니다. |
| `set shiftwidth=4` | 들여쓰기/내어쓰기(`>>`/`<<`) 시 **4칸**을 사용하도록 설정합니다. |
| `set expandtab` | `Tab` 키를 누르면 탭 문자 대신 **공백**이 입력되도록 설정합니다. |
| `set autoindent` | 이전 줄의 들여쓰기를 다음 줄에도 **자동으로 적용**합니다. |

### 4\. 기타 유틸리티

| 설정 | 설명 |
| :--- | :--- |
| `set backspace=indent,eol,start` | **Backspace** 키가 들여쓰기, 줄 끝(EOL), 입력 시작 위치 등에서 모두 정상적으로 작동하도록 설정합니다. (매우 중요) |
| `set encoding=utf-8` | 파일 인코딩을 **UTF-8**로 설정하여 한글 깨짐을 방지합니다. |
| `set fileformats=unix,dos` | 파일 형식 감지 시 \*\*unix(LF)\*\*를 우선하고 다음으로 \*\*dos(CRLF)\*\*를 시도하도록 설정합니다. |

-----

## 📜 예시: 권장 `.vimrc` 내용
* **`~/.vimrc` 파일에 저장 후 Vim 재시작 시 적용**

```vim
" ==========================================================
" 1. 기본 설정 (Basic Settings)
" ==========================================================
set encoding=utf-8
set fileformats=unix,dos
set backspace=indent,eol,start " Backspace 정상 작동 설정
set history=1000 " 명령어 기록을 1000개까지 저장
set mouse=a " 마우스 사용 활성화 (터미널에서도 가능)

" ==========================================================
" 2. 화면 표시 (Display)
" ==========================================================
set number " 줄 번호 표시
set ruler " 우측 하단에 커서 위치 표시
syntax on " 구문 강조 활성화

" ==========================================================
" 3. 검색 및 하이라이트 (Search)
" ==========================================================
set hlsearch " 검색 결과 하이라이트
set incsearch " 점진적 검색
set ignorecase " 검색 시 대소문자 무시
set smartcase " 검색어에 대문자 포함 시 대소문자 구분

" ==========================================================
" 4. 탭 및 들여쓰기 (Tab/Indent)
" ==========================================================
set tabstop=4 " 탭 너비 4칸
set shiftwidth=4 " 들여쓰기 너비 4칸
set expandtab " 탭 대신 공백 사용
set autoindent " 자동 들여쓰기
```

